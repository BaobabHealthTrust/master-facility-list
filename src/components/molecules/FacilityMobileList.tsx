import React from "react";

const FacilityMobileList = () => {
  return <div />;
};

export default FacilityMobileList;
type Props = {
  data: {
    id: number;
    name: string;
    status: string;
    district: string;
  };
  onClick: Function;
};
